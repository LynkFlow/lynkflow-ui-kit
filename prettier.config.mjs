// Thin extends of the shared Prettier config -- see .claude/rules/tooling.md.
// No repo-specific overrides needed; if one is ever needed, add it here
// rather than forking @lynkflow/config/prettier.
export { default } from "@lynkflow/config/prettier";
